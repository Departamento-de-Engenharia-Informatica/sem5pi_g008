describe('Basic Test', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});

describe('Basic Test Fail', () => {
  it('should fail', () => {
    expect(false).toBe(true);
  });
});
